# 🧠 NeuroTask AI
### AI-Powered Project & Task Management Platform

![Python](https://img.shields.io/badge/Python-3.11-blue?logo=python)
![FastAPI](https://img.shields.io/badge/FastAPI-0.111-009688?logo=fastapi)
![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-2.0-red)
![Deploy](https://img.shields.io/badge/Deploy-Render.com-46E3B7?logo=render)

A full-stack AI-powered project management platform built with **FastAPI** (Python), **SQLAlchemy**, **JWT authentication**, **WebSockets**, and a beautiful dark-mode SPA frontend — all deployable with zero manual configuration.

---

## ✨ Features

| Feature | Details |
|---|---|
| 🔐 **JWT Auth** | Secure register/login with bcrypt password hashing |
| 🧠 **AI Priority Scoring** | Smart task scoring using weighted heuristics (due date, priority, complexity) |
| 📊 **Analytics Dashboard** | Productivity score, completion rates, trend charts |
| 🗂 **Kanban Board** | Drag-friendly task board with 4-column workflow |
| 💬 **Comments & Subtasks** | Nested subtasks with progress tracking + comment threads |
| ⚡ **WebSocket Live Sync** | Real-time notifications via WebSocket |
| 📈 **Chart.js Visualizations** | Priority/status doughnuts + weekly trend line chart |
| ⬇ **CSV Export** | Export tasks per project to CSV |
| 🎨 **Beautiful Dark UI** | Responsive SPA — no external CSS framework needed |
| 🚀 **One-Click Deploy** | `render.yaml` auto-deploys on every push to `main` |

---

## 🏗 Tech Stack

**Backend**
- **FastAPI** — High-performance async REST API
- **SQLAlchemy 2.0** — ORM with SQLite (dev) / PostgreSQL (prod)
- **python-jose** — JWT token generation & validation
- **passlib/bcrypt** — Secure password hashing
- **WebSockets** — Real-time bidirectional communication

**Frontend**
- Vanilla JS Single Page Application (zero build step)
- Chart.js for analytics visualizations
- Custom CSS design system (dark mode)

**DevOps**
- GitHub Actions CI/CD (lint + smoke test on every push)
- Render.com auto-deploy via `render.yaml`

---

## 🚀 Deploy in 3 Steps (No Terminal Needed)

### 1️⃣ Fork / Upload to GitHub
Upload all files to a new GitHub repository.

### 2️⃣ Connect to Render.com
1. Go to [render.com](https://render.com) → **New Web Service**
2. Connect your GitHub repo
3. Render auto-detects `render.yaml` — click **Deploy**

### 3️⃣ Done! ✅
Your app is live at `https://neurotask-ai.onrender.com` in ~2 minutes.
The `SECRET_KEY` is auto-generated by Render. No manual env vars needed.

---

## 🔧 Local Development

```bash
# Clone & install
git clone https://github.com/YOUR_USERNAME/neurotask-ai
cd neurotask-ai
pip install -r requirements.txt

# Run
python main.py
# → Open http://localhost:8000
```

---

## 📡 API Documentation

Interactive Swagger docs auto-generated at:
- **`/docs`** — Swagger UI
- **`/redoc`** — ReDoc

### Key Endpoints

| Method | Endpoint | Description |
|---|---|---|
| POST | `/api/auth/register` | Register new user |
| POST | `/api/auth/login` | Login → JWT token |
| GET | `/api/projects` | List user's projects |
| POST | `/api/projects` | Create project |
| GET | `/api/projects/{id}/tasks` | List tasks (sorted by AI score) |
| POST | `/api/projects/{id}/tasks` | Create task (AI score auto-calculated) |
| PATCH | `/api/projects/{id}/tasks/{id}` | Update task |
| GET | `/api/analytics` | Full analytics payload |
| GET | `/api/projects/{id}/tasks/export/csv` | Export tasks as CSV |
| WS | `/ws/{user_id}` | WebSocket real-time channel |

---

## 🧠 AI Scoring Algorithm

Tasks are scored 0–100 based on:

```
score = priority_weight (10-90)
      + due_date_urgency (0-40)
      + complexity_bonus (0-10)
      + engagement_bonus (0-15)
      + status_adjustment (-100 to 0)
```

Higher scores = more urgent/important. Tasks in the kanban board are sorted by AI score by default.

---

## 📁 Project Structure

```
neurotask/
├── main.py              # FastAPI app + WebSocket + static serving
├── database.py          # SQLAlchemy engine & session
├── models.py            # DB models (User, Project, Task, Subtask, Comment, Activity)
├── schemas.py           # Pydantic request/response schemas
├── auth.py              # JWT helpers + password hashing
├── ai_engine.py         # AI priority scoring algorithm
├── routers/
│   ├── auth.py          # /api/auth/* routes
│   ├── projects.py      # /api/projects/* routes
│   ├── tasks.py         # /api/projects/{id}/tasks/* routes
│   └── analytics.py     # /api/analytics route
├── static/
│   └── index.html       # Full SPA frontend (vanilla JS + Chart.js)
├── requirements.txt
├── render.yaml          # Auto-deploy config
└── .github/
    └── workflows/
        └── ci.yml       # GitHub Actions CI/CD
```

---

## 👨‍💻 Author

Built as a portfolio project showcasing full-stack Python development with FastAPI, real-time features, AI-enhanced UX, and modern DevOps practices.

---

*⭐ Star this repo if you find it useful!*
